# fixtures（本地·不入库）

> 本目录的真实文档**不提交到仓库**（仓库 PUBLIC，且含真实学生论文/姓名，属隐私与版权）。
> `.gitignore` 已设 `fixtures/*` + `!fixtures/README.md`：只有本说明入库，文档留本机。
> 另一窗口在同一台机器上开发即可直接用本地文件；他人 clone 需自备同名文件。

用户 2026-06-17 放入，分两套互不相同的格式：

## 格式① 东北农业大学·本科毕业论文 2025（运行期主链）
| 文件 | 角色 |
|---|---|
| `农大格式要求.pdf` | 规范源（PDF 原文） |
| `农大.json` | 已抽规范（结构化规则）→ 编译成 `templates/neau-bachelor-thesis-2025.yaml` |
| `论文（三稿）.docx` | **待排输入**（绿色信贷·本科毕业论文，边距10mm、混乱样式、7表）→ P1 主链输入 |

## 格式② 新疆工程学院·毕业设计模板 2023（建设期 / 第二格式）
| 文件 | 角色 |
|---|---|
| `15毕业设计模板20231008.doc` / `.docx` | 模板/母版（边距25.4/31.7mm、声明页）；`.docx` 为本机 Word COM 转换产物 |
| `马晓倩毕业设计论文(1).docx` | **已排好样本**（医疗废水设计，套上述模板）→ 当"排好"对照真值 + 建设期编译器输入 |

> 引擎通过 `engine/config.py: FIXTURES_DIR` 读取本目录。
